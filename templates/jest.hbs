describe('{{moduleName}}', () => {
{{#each functions}}
  test('should
  {{description}}', () => { expect({{name}}({{#each params}}{{this}}{{#unless
      @last
    }}, {{/unless}}{{/each}})).toBe(expectedResult); });
{{/each}}
});